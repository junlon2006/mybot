# 安全策略

> [English](SECURITY.md) | 简体中文

## 受支持的版本

仅对最新的 release-candidate 标签评估安全修复。当前没有版本提供稳定的生产支持承诺。

## 报告漏洞

请使用仓库 Security 标签页与 GitHub 私有漏洞报告（如可用）。未公开的漏洞请勿开公共
issue。报告请包含受影响版本、平台、影响、复现步骤与建议缓解措施。绝不要包含在线
凭据或用户数据。

本社区项目没有响应 SLA。应在修复或缓解可用后协调披露。

## 传输安全

- 默认启用 HTTPS。平台 TLS 后端必须验证证书链与主机名、发送 SNI，并使用受维护的
  信任库。
- 除非在隔离的本地开发环境中显式编译 `MYBOT_ALLOW_INSECURE_HTTP=ON`，否则明文 HTTP
  会被拒绝。不得在设备或发布构建中启用。

## 已知安全限制

- Linux 文件后端不是生产级秘密存储。
- 第三方安全通告与许可证需单独审查。
